const tab1 = ['Hello', 'je', 'suis', 'un', 'tableau'];
const tab2 = [2, 4, 5];
const tab3 = [true, 0, false];

/**
 * Retourner le premier élément du tableau que l'on vous donne en paramètre. 
 */
function last(tableau){

}

console.log(last(tab1)); // Expected Output: 'tableau'
console.log(last(tab2)); // Expected Output: 5
console.log(last(tab3)); // Expected Output: false
