const tab1 = ['Hello', 'je', 'suis', 'un', 'tableau'];
const tab2 = [2, 4, 5];
const tab3 = [true, 0, false];

/**
 * Retourner le premier élément du tableau que l'on vous donne en paramètre. 
 */
function first(tableau){

}

console.log(first(tab1)); // Expected Output: 'Hello'
console.log(first(tab2)); // Expected Output: 2
console.log(first(tab3)); // Expected Output: true
